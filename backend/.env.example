# MongoDB Configuration
# Replace with your actual MongoDB Atlas credentials
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/YOUR_DATABASE_NAME?retryWrites=true&w=majority&appName=Cluster0

# Server Configuration  
PORT=8000

# Frontend URL (if needed for CORS)
CLIENT_URL=http://localhost:3000

# Instructions:
# 1. Go to MongoDB Atlas (https://cloud.mongodb.com/)
# 2. Go to Database Access and create a new user or check existing user credentials
# 3. Go to Network Access and add your IP address (or 0.0.0.0/0 for testing)
# 4. Get your connection string from Database > Connect > Connect your application
# 5. Replace YOUR_USERNAME, YOUR_PASSWORD, and YOUR_DATABASE_NAME with actual values
# 6. Make sure the password is URL encoded (special characters need encoding)
